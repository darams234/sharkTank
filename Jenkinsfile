pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                // Just printing a message
                echo 'Hello, this is a simple Jenkins pipeline!'
            }
        }
    }
}
